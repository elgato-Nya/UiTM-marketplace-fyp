// Barrel export for User model
const User = require("./user.model");
const Address = require("./address.schema");
const Merchant = require("./merchant.schema");

module.exports = {
  User,
  Address,
  Merchant,
};
